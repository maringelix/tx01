name: ☸️ Deploy to EKS

on:
  workflow_dispatch:

env:
  AWS_REGION: us-east-1

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: 📋 Info
        run: echo "EKS deployment workflow - Coming soon"
